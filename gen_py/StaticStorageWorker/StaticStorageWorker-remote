# -*- coding: utf-8 -*-
#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from gen_py.StaticStorageWorker import StaticStorageWorker
from gen_py.StaticStorageWorker.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  FileId uploadFile(InputFileDto file)'
  print '  string getFileUrl(FileRefDto fileId)'
  print '   getFileUrlBatch( fileIds,  formats)'
  print '  void deleteFile(FileId fileId)'
  print '  FileId uploadPictureWithProfile(InputFileDto file, string profile)'
  print '  void refreshPictureWithFormats(FileId fileId, bool force)'
  print '  void applyPictureTransformations(FileId fileId, FullTransformationDto transformation)'
  print '  RefreshAllResultDto refreshAllPictures(string profile)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = StaticStorageWorker.Client(protocol)
transport.open()

if cmd == 'uploadFile':
  if len(args) != 1:
    print 'uploadFile requires 1 args'
    sys.exit(1)
  pp.pprint(client.uploadFile(eval(args[0]),))

elif cmd == 'getFileUrl':
  if len(args) != 1:
    print 'getFileUrl requires 1 args'
    sys.exit(1)
  pp.pprint(client.getFileUrl(eval(args[0]),))

elif cmd == 'getFileUrlBatch':
  if len(args) != 2:
    print 'getFileUrlBatch requires 2 args'
    sys.exit(1)
  pp.pprint(client.getFileUrlBatch(eval(args[0]),eval(args[1]),))

elif cmd == 'deleteFile':
  if len(args) != 1:
    print 'deleteFile requires 1 args'
    sys.exit(1)
  pp.pprint(client.deleteFile(eval(args[0]),))

elif cmd == 'uploadPictureWithProfile':
  if len(args) != 2:
    print 'uploadPictureWithProfile requires 2 args'
    sys.exit(1)
  pp.pprint(client.uploadPictureWithProfile(eval(args[0]),args[1],))

elif cmd == 'refreshPictureWithFormats':
  if len(args) != 2:
    print 'refreshPictureWithFormats requires 2 args'
    sys.exit(1)
  pp.pprint(client.refreshPictureWithFormats(eval(args[0]),eval(args[1]),))

elif cmd == 'applyPictureTransformations':
  if len(args) != 2:
    print 'applyPictureTransformations requires 2 args'
    sys.exit(1)
  pp.pprint(client.applyPictureTransformations(eval(args[0]),eval(args[1]),))

elif cmd == 'refreshAllPictures':
  if len(args) != 1:
    print 'refreshAllPictures requires 1 args'
    sys.exit(1)
  pp.pprint(client.refreshAllPictures(args[0],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
